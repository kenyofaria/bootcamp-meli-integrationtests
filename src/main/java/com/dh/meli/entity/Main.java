package com.dh.meli.entity;

public class Main {

	
	public static void main(String[] args) {
		
		//Funcionario funcionario = new Funcionario();
		//funcionario.getValorTotalEmReajustes();
		//BigDecimal total = funcionario.getReajustes().stream().map(r -> r.getValor()).reduce(new BigDecimal(0), BigDecimal::add);
		
//		
//		Terceirizado terceirizado = new Terceirizado(new DadosPessoais("", Cargo.ANALISTA, new BigDecimal(1000), LocalDate.now()), "DH");
//		
//		ReajusteSalarioService reajusteSalarioService = new ReajusteSalarioService(Arrays.asList(new ValidacaoValorAumento()));
//		reajusteSalarioService.reajustaSalario(terceirizado, new BigDecimal(500000));
	}
}
